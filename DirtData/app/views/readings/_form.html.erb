<%= form_with(model: reading, local: true) do |form| %>
  <% if reading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reading.errors.count, "error") %> prohibited this reading from being saved:</h2>

      <ul>
      <% reading.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :time %>
    <%= form.datetime_select :time, id: :reading_time %>
  </div>

  <div class="field">
    <%= form.label :moisture %>
    <%= form.text_field :moisture, id: :reading_moisture %>
  </div>

  <div class="field">
    <%= form.label :light %>
    <%= form.text_field :light, id: :reading_light %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
